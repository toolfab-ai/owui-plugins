[project]
name = "memory-islands"
version = "0.1.0"
description = "Dynamic, folder-scoped context and memory isolation for Open WebUI"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
    "httpx>=0.27",
]
