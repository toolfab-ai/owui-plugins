[project]
name = "memory-islands-action"
version = "0.2.0"
description = "Manage folder-scoped memories for the Memory Islands filter from a Rich UI panel"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
    "httpx>=0.27",
]